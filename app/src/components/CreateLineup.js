import React from 'react';

const CreateLineup = props => {
  return (
    <div className="CreateLineupContainer">
      <h1>Pick Meet</h1>
      <h1>Create Grid with "checkboxes" for each event</h1>
      <h1>Each Swimmer on the team can only have 4 check boxes with 2 individuals</h1>
      <h1>When Submit the lineup is updated</h1>
    </div>
  );
};

export default CreateLineup;
